name: SonarCloud Analysis

on:
  push:
    branches: [ main, master, enhancement-ui ]
  pull_request:

jobs:
  sonarcloud:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@v2
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          projectBaseDir: .
          args: >
            -Dsonar.organization=mohammad-arslan
            -Dsonar.projectKey=Mohammad-Arslan_sis
            -Dsonar.ci=true