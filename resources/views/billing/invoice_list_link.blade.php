<a type="button" class="text-secondary show-modal" data-url="{{ route('student-invoices.show', $row['invoice']['id']) }}"
    data-target="#invoiceDetailModal">{{ $row['invoice']['invoice_no'] }}</a>
